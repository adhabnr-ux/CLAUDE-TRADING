# Schema examples

`trade-plan.json` demonstrates structure only. Its `.invalid` earnings URL is a
deliberate placeholder and the record is absent from the checked-in trusted
calendar. It cannot authorize an order. An executable buy must copy the exact
date, verification timestamp, and HTTPS source from a fresh human-reviewed
`config/earnings-calendar.json` entry.
